import * as React from 'react'
import { IAppState } from '../states/appState';

export default class Tourmanents extends React.Component<{appState: IAppState}> {
  render() {
    return (
      <div>Tournament component</div>
    )
  }
}